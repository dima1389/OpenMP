/*
 * File:
 *   omp_<topic>.c
 *
 * Purpose:
 *   Demonstrate <controlled-term OpenMP concept>.
 *
 * Description:
 *   This example demonstrates <controlled-term OpenMP mechanism> under
 *   <controlled-term condition>.
 *   Observable outcome: <controlled-term behavior that can be observed or measured>.
 *
 * Key concepts:
 *   - <data sharing | synchronization | scheduling | reduction | tasking>
 *   - <deterministic | nondeterministic | blocking | non-blocking behavior>
 *   - <performance effect: overhead | load imbalance | locality>
 *
 * Algorithm / workflow (high level):
 *   1) Initialize input data
 *   2) Execute OpenMP parallel region
 *   3) Synchronize and/or combine partial results
 *   4) Report result or timing
 *
 * OpenMP features used (list only those actually used in this file):
 *   - <parallel | for | simd | reduction | task | barrier | critical | atomic>
 *   - <omp_get_thread_num | omp_get_num_threads | omp_get_wtime>
 *
 * Compilation:
 *   gcc -O2 -Wall -Wextra -Wpedantic -g -fopenmp omp_<topic>.c -o omp_<topic>
 *
 * Execution:
 *   ./omp_<topic> [arguments]
 *
 * Inputs:
 *   - Command-line arguments: <N | none>
 *   - Environment variables (optional): OMP_NUM_THREADS, OMP_SCHEDULE
 *
 * References:
 *   - OpenMP API Specification: <section or construct name>
 */
